#### 01-major_class_samplelist.R
The code generates the all sample name information for hmC and mC for each major class and subclass.

#### "02-allcools_merge_major_class.sh", "03-allcools_merge_subclass.sh"
These three scripts are for merging the allc files at different clustering levels.
These are the bash files that actually carry out the merging process using the generated allc tables.